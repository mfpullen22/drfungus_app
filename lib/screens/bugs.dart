import "package:flutter/material.dart";

class BugsScreen extends StatelessWidget {
  const BugsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(child: Text("Bugs Screen"));
  }
}
