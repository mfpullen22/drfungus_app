import "package:flutter/material.dart";

class TrialsScreen extends StatelessWidget {
  const TrialsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Clincial Trials"),
    );
  }
}
