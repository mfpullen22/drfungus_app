import "package:flutter/material.dart";

class DrugsScreen extends StatelessWidget {
  const DrugsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(child: Text("Drugs Screen"));
  }
}
